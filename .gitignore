node_modules/
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
.env
.env.local
*.log
.vscode/
.idea/

# Packaging output (generated by scripts/pack.mjs).
# Note: docs/ is intentionally NOT ignored — a fresh clone should have a
# working API index without needing a build step. CI enforces it stays current.
dist/
